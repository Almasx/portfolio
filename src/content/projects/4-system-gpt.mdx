---
title: "System GPT \nAI roadmap generation"
tags: ["React", "XState", "Redis", "AI", "TypeScript", "Next.JS", "Tailwind"]
slug: "system-gpt"
---

import { TextBlock, Title, Tags } from "~/layouts/content";
import { Block } from "~/components/block";

export const content = (
  <img
    src="/assets/web-4.svg"
    alt="System GPT AI roadmap generation"
    class="w-full rounded-xl"
  />
);

{props.view === "sm" &&

<Block slug={frontmatter.slug} {...frontmatter.data}>
  {content}
</Block>
}

{props.view === "lg" && (

<div class="relative md:rounded-3xl rounded-2xl overflow-y-auto overflow-x-hidden md:p-11 p-6 flex flex-col text-neutral-400">
  <Title>{frontmatter.title}</Title>
  <TextBlock>
    I independently developed an AI-driven app that generates personalized
    goals, breaks them down into tasks, and creates a dynamic schedule tailored
    to the user’s needs. The app offers a flexible and structured approach to
    productivity. I used XState to handle the complex workflows and Redis as the
    database.
  </TextBlock>
  <Tags tags={frontmatter.tags} />
  <div class="top-1/3 md:absolute md:w-1/2 scale-110 origin-center mt-[10%]  left-1/2">
    {content}
  </div>
</div>
)}
