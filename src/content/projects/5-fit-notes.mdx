---
title: "Fitnotes"
skills: ["Expo", "Tailwind", "TypeScript", "React Native"]
tags: ["2024", "Personal Project", "UI/UX", "Mobile", "React Native", "Expo"]
slug: "fitnotes"
hook: "Simple, usable workout tracking"
---

import { TextBlock, Title, Skills } from "~/layouts/content";
import { Block } from "~/components/block";

export const content = <img src="/assets/web-6.png" />;

<Block
  slug={frontmatter.slug}
  tags={frontmatter.tags}
  hook={frontmatter.hook}
  className="!bg-black !aspect-[2/3]"
  size="sm"
  active={props.view === "sm"}
  client:visible
>
  <div className="absolute bottom-20 left-20 w-full">{content}</div>
</Block>

{props.view === "lg" && (

<div className="relative md:rounded-3xl rounded-2xl overflow-y-auto overflow-x-hidden md:p-11 p-6 flex flex-col bg-black text-neutral-400">
  <Title className="text-white">{frontmatter.title}</Title>
  <TextBlock>
    Right now, it’s just a concept. I’ve been using FitNotes on Android for a
    while, and there’s nothing like it on iOS that’s both free and usable.
    That’s why I’m building my own. It’s a simple, effective workout tracker
    that shows progress and works without frustration. I’m trying out some new
    design ideas and plan to keep improving it over time to make it something
    people actually want to use.
  </TextBlock>
  <Skills skills={frontmatter.skills} className="!mb-20 md:!mb-0" />
  <div className="md:absolute bottom-20 -right-20">{content}</div>
</div>
)}
