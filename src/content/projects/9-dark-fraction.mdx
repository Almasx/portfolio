---
title: "Dark Fraction"
skills: ["React", "TypeScript", "Next.JS", "Tailwind"]
tags: ["2023", "Dark Fraction", "CMS", "Frontend", "Next.js", "VK Integration"]
slug: "dark-fraction"
hook: "Custom CMS for 25k+ manga community"
---

import { TextBlock, Title, Skills } from "~/layouts/content";
import { Block } from "~/components/block";

export const content = <img src="/assets/web-10.svg" alt="Dark Fraction" />;

<Block
  slug={frontmatter.slug}
  tags={frontmatter.tags}
  hook={frontmatter.hook}
  className="!bg-black !min-h-60 !aspect-auto"
  active={props.view === "sm"}
  client:visible
>
  <div className="absolute top-12 left-12">{content}</div>
</Block>

{props.view === "lg" && (

<div className="relative md:rounded-3xl rounded-2xl  overflow-y-auto overflow-x-hidden md:p-11 p-6 flex flex-col bg-black text-neutral-500">
  <Title className="text-white">{frontmatter.title}</Title>
  <TextBlock>
    I built a custom CMS and catalog platform for Dark Fracktion, a VK group
    with 25,000+ members translating mangas into Russian. Using Next.js, I
    created tools for admins to manage content efficiently and a user-friendly
    catalog for members. Payment was handled via VK Donut integration.
  </TextBlock>
  <Skills skills={frontmatter.skills} highlight="hover:text-blue-500" />
  <div className="absolute top-1/2 left-20 md:left-1/2 w-[200%] md:w-full">
    {content}
  </div>
</div>
)}
